/*
exports.run = (client, message, args) => {

	if(!client.scoreCard.has(`${message.author.id}`)) {
		message.channel.send("You do not have a card in this guild, do \`,join-guild\` to get one.");
		return;
	};

	

};
*/